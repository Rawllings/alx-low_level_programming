#include <stdio.h>
/**
 * main - print all numbers of base 16 in lowercase
 * return: 0
 */
int main(void)
{
	int num;
	char la;

	for (num = 0; num < 10; num++)
		putchar((nu % 10) + '0');
	for (la = 'a'; la <= 'f'; la++)
		putchar(la);

	putchar('\n');

	return (0);
}
